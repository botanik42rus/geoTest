<UserControl x:Class="geoTest.SaveUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:geoTest"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">

    <Border BorderThickness="2" BorderBrush="LightGray">
        <Grid Background="#f0f5fa">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Идёт сохранение в файл. Пожалуйста подождите." HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="24"
                   TextAlignment="Center" TextWrapping="WrapWithOverflow"/>
            <ProgressBar Grid.Row="1" Margin="7" IsIndeterminate="True"/>
        </Grid>
    </Border>
</UserControl>
